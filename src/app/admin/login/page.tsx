'use client';
import { FormEvent, useState } from 'react';
import { signInWithEmailAndPassword } from 'firebase/auth';
import { Scale } from 'lucide-react';
import { auth } from '@/lib/firebase';
import { useAdminAuth } from '@/components/admin/AdminAuth';
export default function Login(){const {user,authorized}=useAdminAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{await signInWithEmailAndPassword(auth,email,password)}catch{setError('Unable to sign in. Check your credentials.')}}return <main className="grid min-h-screen place-items-center bg-[#080b12] px-6 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-white/10 bg-[#0c1019] p-8"><span className="mb-8 grid h-12 w-12 place-items-center rounded-xl bg-blue-600"><Scale/></span><p className="text-xs font-semibold uppercase tracking-[.2em] text-blue-400">Secure administration</p><h1 className="mt-2 text-3xl font-semibold">Welcome back</h1>{user&&!authorized&&<p className="mt-5 rounded-lg bg-amber-500/10 p-3 text-sm text-amber-300">This account does not have a beam_admin or partner_admin claim.</p>}<label className="mt-8 grid gap-2 text-sm text-slate-300">Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="rounded-lg border border-white/10 bg-slate-950 px-4 py-3"/></label><label className="mt-4 grid gap-2 text-sm text-slate-300">Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} className="rounded-lg border border-white/10 bg-slate-950 px-4 py-3"/></label><button className="mt-6 w-full rounded-lg bg-blue-600 py-3 font-semibold hover:bg-blue-500">Sign in</button>{error&&<p className="mt-4 text-sm text-red-400">{error}</p>}</form></main>}

